# Third-party notices

Threshold's original source and original SVG/CSS artwork are MIT licensed.

Runtime dependencies are installed from npm, pinned in `package-lock.json`, and retain their own license files:

- `@modelcontextprotocol/sdk`: official Model Context Protocol TypeScript SDK, MIT.
- `express`: Express web framework, MIT.
- `zod`: schema validation and JSON Schema conversion, MIT.
- Transitive dependencies retain their corresponding notices in the installed packages.

The Streamable HTTP integration follows the official SDK's documented API and request-lifetime pattern. The application, task model, scheduling, UI, and tests are original work. No third-party music, film, photographs, fonts, logos, real household data, or earlier hackathon code is bundled. System fonts and original SVG are used.

Alexa+ and Claude are referenced descriptively. This project is not sponsored, endorsed, or certified by their owners. External services remain subject to their providers' terms.
